# cdw-semantic-segmentation

Semantic segmentation of Construction & Demolition Waste (CDW) images (10 classes).

## Dataset notes
- Ground-truth masks use integer labels.
- Label `0` = unlabeled / ignore.
- Material classes are labeled `1..10`.

## Status
Project setup (repo initialization). Implementation in progress.